<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="test.mapper.MemberMapper">

    <!-- 로그인 정보 조회 -->
    <select id="loginCheck" parameterType="string" resultType="test.mapper.MemberDTO">
        SELECT 
            MEM_CODE AS memCode,
            MEM_ID   AS memId,
            MEM_PW   AS memPw
        FROM MEM
        WHERE MEM_ID = #{userId}
    </select>

	<!-- 아이디 중복 확인 -->
    <select id="idCheck" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM MEM
        WHERE MEM_ID = #{memId}
    </select>
    <!-- 회원가입 -->
     <insert id="signUp" parameterType="test.mapper.MemberDTO" >
     INSERT INTO MEM (
        MEM_ID, MEM_CODE, MEM_PW, MEM_NAME, MEM_TEL, MEM_EMAIL, MEM_POSTNUM, MEM_JIBUNADDRESS, MEM_ADDRESS
    ) VALUES (
        #{memId}, #{memCode}, #{memPw}, #{memName}, #{phoneNumber}, #{emailId}, #{postNum},#{roadAddress}, #{addressDetail}
    )
    </insert>
    <select id="selectMypage" resultType="test.mapper.MemberDTO">
   		SELECT MEM_ID AS memId
   		,MEM_PW AS memPw
   		,MEM_CODE AS memCode
   		,MEM_NAME AS memName
   		FROM MEM
   		WHERE 1=1
   		AND MEM_ID = #{memId}
   </select>
     
</mapper>